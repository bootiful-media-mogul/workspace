# workspace

clone this to incept your local development environment.



## usage on fresh clone 
```
cd workspace
./setup_mogul.sh
```


## configuring the environment 
* make sure to open a new shell then cd `~/code/mogul`
* run `bw-unlock`
```
source env.sh 
```
